import {
    FETCHING_POSTS,
    DID_FETCH_POSTS,
} from '../actions'

export const home = (state = {}, action) => {
    switch(action.type) {
        case 
    }
} 

export const posts = (state = {}, action) => {
    switch(action.type) {
        case CREATE_POST:
            return state
        default:
            return state
    }
}

export default posts